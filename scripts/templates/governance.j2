---
title: "Governance"
description: "Governance policies and expectations for the product framework"
date: 2024-01-01
draft: false
weight: 30
---

{% raw %}{{< rawhtml >}}{% endraw %}
<style>
{{ css_content }}
</style>

<div class="governance-hero">
    <div class="container">
        <div class="hero-content">
            <div class="hero-icon">
                {{ governance_icon }}
            </div>
            <h1 class="hero-title">Secure Product Model Governance</h1>
            <p class="hero-subtitle">Governance policies and expectations for the product framework</p>
            <p class="hero-description">
                The foundation of your security program. These {{ governance_count }} governance items
                establish the policies, oversight processes, scope definitions, and protocols that guide security
                decision-making across your organization and ensure compliance with regulatory requirements.
            </p>
        </div>
    </div>
</div>

<div class="governance-grid-section">
    <div class="container">
        <h2 class="section-title">Governance Categories</h2>
        <p class="section-subtitle">Browse all {{ governance_count }} governance items organized into {{ category_count }} key categories</p>

        <div class="governance-filter">
            <button class="filter-btn active" data-category="all">All Governance</button>
            <button class="filter-btn" data-category="policy">Policies</button>
            <button class="filter-btn" data-category="oversight">Oversight</button>
            <button class="filter-btn" data-category="scopedefinition">Scope Definition</button>
            <button class="filter-btn" data-category="protocol">Protocols</button>
        </div>

        <div class="governance-grid" id="governanceGrid">
            {% for item in governance_items %}
            <div class="governance-card" data-category="{{ item.category }}">
                <div class="card-header">
                    <div class="category-badge category-{{ item.category }}">
                        <div class="category-icon">
                            {{ category_icons[item.category] }}
                        </div>
                        <span class="category-name">{{ item.category_display }}</span>
                    </div>
                </div>
                <div class="card-body">
                    <h3 class="card-title">
                        <a href="/governance/{{ item.category }}/{{ item.slug }}/">{{ item.expectation or item.name }}</a>
                    </h3>
                    <p class="card-id">ID: {{ item.id }}</p>
                    {% if item.details %}
                    <p class="card-description">{{ item.details[:150] }}{% if item.details|length > 150 %}...{% endif %}</p>
                    {% endif %}
                </div>
                <div class="card-footer">
                    <a href="/governance/{{ item.category }}/{{ item.slug }}/" class="card-link">View Details →</a>
                </div>
            </div>
            {% endfor %}
        </div>

        <div class="governance-stats">
            <div class="stats-grid">
                {% for category, items in categories.items() %}
                <div class="stat-card">
                    <div class="stat-icon">{{ category_icons[category] }}</div>
                    <div class="stat-content">
                        <h3 class="stat-number">{{ items|length }}</h3>
                        <p class="stat-label">{{ items[0].category_display if items else category.title() }}</p>
                        <a href="/governance/{{ category }}/" class="stat-link">View all →</a>
                    </div>
                </div>
                {% endfor %}
            </div>
        </div>
    </div>
</div>

<div class="governance-footer-section">
    <div class="container">
        <div class="footer-content">
            <h2>Ready to implement governance?</h2>
            <p>These governance items provide the framework for building a robust security program that meets compliance requirements and protects your organization.</p>
            <div class="footer-stats">
                <div class="footer-stat">
                    <strong>{{ governance_count }}</strong>
                    <span>Governance Items</span>
                </div>
                <div class="footer-stat">
                    <strong>{{ category_count }}</strong>
                    <span>Categories</span>
                </div>
                <div class="footer-stat">
                    <strong>100%</strong>
                    <span>Framework Coverage</span>
                </div>
            </div>
        </div>
    </div>
</div>

<script>
{{ js_content }}
</script>
{% raw %}{{< /rawhtml >}}{% endraw %}
