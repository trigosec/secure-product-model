---
title: "{{ item.title or item.name }}"
description: "{{ item.description[:150] }}{% if item.description and item.description|length > 150 %}...{% endif %}"
date: 2024-01-01
draft: false
governance_id: "{{ item.id }}"
governance_category: "{{ item.category }}"
---

{% raw %}{{< rawhtml >}}{% endraw %}
<style>
{{ css_content }}
</style>

<div class="governance-hero">
    <div class="container">
        <div class="hero-content">
            <div class="hero-icon">
                {{ category_icon }}
            </div>
            <h1 class="hero-title">{{ item.title or item.name }}</h1>
            <nav class="breadcrumb">
                <a href="/governance/" class="breadcrumb-link">Governance</a>
                <span class="breadcrumb-separator">→</span>
                <a href="/governance/{{ item.category }}/" class="breadcrumb-link">{{ item.category_display }}</a>
            </nav>
            {% if item.description %}
            <p class="hero-description">{{ item.description }}</p>
            {% endif %}
        </div>
    </div>
</div>

<div class="governance-item-content">
    <div class="container">
        <div class="content-grid">
            <main class="main-content">
                {% if item.description %}
                <section class="content-section">
                    <h2 class="section-title">
                        <span class="section-icon">📋</span>
                        Description
                    </h2>
                    <div class="section-content">
                        <p>{{ item.description }}</p>
                    </div>
                </section>
                {% endif %}

                <section class="content-section">
                    <h2 class="section-title">
                        <span class="section-icon">🚧</span>
                        Under Construction
                    </h2>
                    <div class="section-content">
                        <div class="construction-notice" style="background: #fff3cd; border: 1px solid #ffeaa7; padding: 1rem; border-radius: 0.5rem; color: #856404;">
                            <p><strong>This page is currently under construction.</strong></p>
                            <p>We are actively working on expanding the content for this governance item. Updates will be published regularly as we continue to develop and refine the governance framework.</p>
                        </div>
                    </div>
                </section>
            </main>

            <aside class="sidebar">
                <div class="sidebar-card">
                    <h3 class="sidebar-title">Compliance Frameworks</h3>
                    {% if item.compliance_frameworks and item.compliance_frameworks|length > 0 %}
                    <div class="compliance-frameworks">
                        {% for framework in item.compliance_frameworks %}
                        <div class="compliance-badge">
                            <span class="framework-name">{{ framework }}</span>
                        </div>
                        {% endfor %}
                    </div>
                    {% else %}
                    <div class="no-frameworks">
                        <p style="color: #666; font-style: italic;">No specific compliance frameworks identified</p>
                    </div>
                    {% endif %}
                </div>
            </aside>
        </div>
    </div>
</div>

<div class="governance-item-footer">
    <div class="container">
        <div class="footer-content">
            <p class="footer-text">
                Part of the <strong>Secure Product Model</strong> governance framework
            </p>
        </div>
    </div>
</div>

<script>
{{ js_content }}
</script>
{% raw %}{{< /rawhtml >}}{% endraw %}
